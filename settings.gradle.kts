plugins {
    // Lädt automatisch ein JDK 25 (Temurin) nach ~/.gradle/jdks, falls lokal nur eine JRE vorhanden ist.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "BankRanking"
